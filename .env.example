PORT=5000
MONGO_URI=
JWT_SECRET=

# Firebase Admin. Use these in production instead of committing a service-account JSON file.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Optional CORS origins, comma-separated.
CLIENT_URL=http://localhost:5173
