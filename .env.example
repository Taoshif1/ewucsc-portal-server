PORT=5000
NODE_ENV=development
MONGO_URI=
JWT_SECRET=

# Firebase project ID used to validate Firebase ID tokens.
# Defaults to smart-deals-37b05 for the current EWUCSC Firebase project.
FIREBASE_PROJECT_ID=smart-deals-37b05

# Comma-separated browser origins allowed to call the API.
CLIENT_URL=http://localhost:5173
LIVE_CLIENT_URL=
ALLOWED_ORIGINS=

# Optional additional comma-separated bootstrap Admin emails.
# Required built-in Admins include the official EWUCSC account.
BOOTSTRAP_ADMIN_EMAILS=
